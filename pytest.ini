[pytest]
asyncio_mode = auto
python_files = test_*.py
python_functions = test_*
python_classes = Test*
addopts =
    --cov=app
    --cov-report=term-missing
    --cov-fail-under=85
    -v
filterwarnings =
    ignore::DeprecationWarning
