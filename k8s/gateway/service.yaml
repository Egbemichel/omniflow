apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: omniflow
spec:
  selector:
    app: gateway

  type: NodePort
  
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
