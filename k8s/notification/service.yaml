apiVersion: v1
kind: Service
metadata:
  name: notification
  namespace: omniflow

spec:
  selector:
    app: notification

  ports:
    - name: http
      port: 8005
      targetPort: 8005

  type: ClusterIP