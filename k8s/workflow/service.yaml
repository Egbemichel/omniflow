apiVersion: v1
kind: Service
metadata:
  name: workflow
  namespace: omniflow

spec:
  selector:
    app: workflow

  ports:
    - name: http
      port: 8003
      targetPort: 8003

  type: ClusterIP